export function createModelWidgetHtml(origin: string) {
  return String.raw`<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <style>
    :root{color-scheme:dark;--ink:#f5f0e5;--muted:#96938b;--line:#343431;--orange:#ff5d2e;--blue:#7388ff;--green:#54c57e}*{box-sizing:border-box}body{margin:0;background:#11110f;color:var(--ink);font-family:ui-sans-serif,system-ui,-apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif}.app{min-height:540px;display:grid;grid-template-columns:minmax(0,1fr) 250px;overflow:hidden;border:1px solid var(--line);border-radius:14px;background:#11110f}.stage{position:relative;min-height:540px}.canvas{position:absolute;inset:0}.canvas canvas{display:block;width:100%;height:100%}.hud{position:absolute;z-index:2;top:14px;left:14px;right:14px;display:flex;align-items:flex-start;justify-content:space-between;pointer-events:none}.title,.dims{padding:9px 11px;border:1px solid #3a3a36;border-radius:7px;background:#11110fcc;backdrop-filter:blur(10px)}.title small{display:block;margin-bottom:4px;color:var(--orange);font:700 8px ui-monospace,monospace;letter-spacing:.08em;text-transform:uppercase}.title strong{font-size:15px}.dims{color:var(--muted);font:650 9px ui-monospace,monospace}.focus{position:absolute;z-index:3;left:14px;bottom:14px;width:36px;height:36px;border:1px solid #3d3d39;border-radius:7px;background:#171715d9;color:#aaa79f;cursor:pointer}.loading{position:absolute;z-index:4;inset:0;display:grid;place-items:center;background:#11110f88;color:var(--muted);font:600 10px ui-monospace,monospace}.side{display:flex;flex-direction:column;padding:18px;border-left:1px solid var(--line);background:#181816}.brand{display:flex;align-items:center;gap:9px;margin-bottom:22px;font-size:12px;font-weight:850;letter-spacing:.12em}.mark{display:grid;place-items:center;width:29px;height:29px;border-radius:7px;background:var(--orange);color:#17120f}.tag{margin-left:auto;color:var(--muted);font:600 8px ui-monospace,monospace}.kicker{color:var(--blue);font:700 8px ui-monospace,monospace;letter-spacing:.1em;text-transform:uppercase}.side h2{margin:7px 0 8px;font-size:20px;line-height:1.05}.side p{margin:0;color:var(--muted);font-size:10px;line-height:1.55}.stats{display:grid;gap:8px;margin-top:18px}.stats span{display:flex;justify-content:space-between;gap:10px;padding:9px;border:1px solid #333330;border-radius:7px;background:#20201d;color:#85827a;font:600 8px ui-monospace,monospace}.stats strong{color:#d6d2ca;font:inherit}.warnings{display:grid;gap:7px;margin-top:12px}.warning{padding:9px;border:1px solid #8b5b2b;border-radius:7px;background:#352617;color:#ffc27d;font:600 8px/1.45 ui-monospace,monospace}.actions{display:grid;gap:8px;margin-top:auto;padding-top:18px}.actions a{min-height:40px;display:flex;align-items:center;justify-content:center;border-radius:7px;text-decoration:none;font-size:10px;font-weight:780}.download{background:var(--orange);color:#21110d}.studio{border:1px solid #444440;color:#d2cec5}.status{display:flex;align-items:center;gap:7px;margin-top:11px;color:#74d897;font:600 8px ui-monospace,monospace}.status i{width:7px;height:7px;border-radius:50%;background:var(--green)}@media(max-width:700px){.app{grid-template-columns:1fr}.stage{min-height:360px}.side{border-left:0;border-top:1px solid var(--line)}.stats{grid-template-columns:1fr 1fr}.actions{grid-template-columns:1fr 1fr}}
  </style>
</head>
<body>
  <div class="app">
    <main class="stage"><div id="canvas" class="canvas"></div><div class="hud"><div class="title"><small>Procedural solid</small><strong id="title">Building model…</strong></div><div id="dims" class="dims">—</div></div><button id="focus" class="focus" aria-label="Frame model">◎</button><div id="loading" class="loading">Loading printable mesh…</div></main>
    <aside class="side"><div class="brand"><span class="mark">▰</span>PRINTA <span class="tag">SPEC 1.0</span></div><span class="kicker">Composable model graph</span><h2 id="side-title">Procedural model</h2><p id="description">Sources, modifiers, transforms, assemblies, and simulations composed into one printable STL.</p><div class="stats"><span>Triangles <strong id="triangles">—</strong></span><span>Volume est. <strong id="volume">—</strong></span></div><div id="warnings" class="warnings"></div><div class="actions"><a id="download" class="download" href="#">Download STL</a><a id="studio" class="studio" href="#">Open studio</a></div><div class="status"><i></i><span id="status">Ready for slicer</span></div></aside>
  </div>
  <script type="module">
    import * as THREE from "https://cdn.jsdelivr.net/npm/three@0.185.0/build/three.module.js";
    import {OrbitControls} from "https://cdn.jsdelivr.net/npm/three@0.185.0/examples/jsm/controls/OrbitControls.js";
    import {STLLoader} from "https://cdn.jsdelivr.net/npm/three@0.185.0/examples/jsm/loaders/STLLoader.js";
    const ORIGIN=${JSON.stringify(origin)},el=id=>document.getElementById(id),scene=new THREE.Scene();scene.background=new THREE.Color("#11110f");const camera=new THREE.PerspectiveCamera(34,1,.1,3000);camera.up.set(0,0,1);camera.position.set(150,-190,135);const renderer=new THREE.WebGLRenderer({antialias:true});renderer.setPixelRatio(Math.min(devicePixelRatio,2));renderer.outputColorSpace=THREE.SRGBColorSpace;renderer.shadowMap.enabled=true;renderer.toneMapping=THREE.ACESFilmicToneMapping;renderer.toneMappingExposure=1.08;el("canvas").appendChild(renderer.domElement);const controls=new OrbitControls(camera,renderer.domElement);controls.enableDamping=true;controls.target.set(0,0,45);scene.add(new THREE.HemisphereLight("#fff5df","#17203b",2.6));const key=new THREE.DirectionalLight("#fff0d7",5.2);key.position.set(-120,-140,220);key.castShadow=true;scene.add(key);const rim=new THREE.DirectionalLight("#7288ff",4);rim.position.set(130,90,140);scene.add(rim);const floor=new THREE.Mesh(new THREE.CircleGeometry(230,96),new THREE.MeshStandardMaterial({color:"#191916",roughness:.86,metalness:.08}));floor.position.z=-.25;floor.receiveShadow=true;scene.add(floor);const grid=new THREE.GridHelper(400,40,"#373732","#272724");grid.rotation.x=Math.PI/2;grid.position.z=.04;scene.add(grid);let mesh=null,token=0;
    function frame(){if(!mesh)return;const box=new THREE.Box3().setFromObject(mesh),sphere=box.getBoundingSphere(new THREE.Sphere()),distance=Math.max(45,sphere.radius/Math.tan(THREE.MathUtils.degToRad(camera.fov/2))*1.2);camera.position.set(sphere.center.x+distance*.78,sphere.center.y-distance,sphere.center.z+distance*.66);camera.near=Math.max(.1,distance/150);camera.far=distance*20;camera.updateProjectionMatrix();controls.target.copy(sphere.center);controls.update()}
    async function show(data){if(!data||!data.stlUrl)return;const current=++token;el("loading").style.display="grid";el("title").textContent=data.name;el("side-title").textContent=data.name;el("description").textContent=data.description||"A composable printable model.";el("dims").textContent=data.widthMm.toFixed(1)+" × "+data.depthMm.toFixed(1)+" × "+data.heightMm.toFixed(1)+" mm";el("triangles").textContent=Number(data.triangles).toLocaleString();el("volume").textContent=(data.volumeEstimateMm3/1000).toFixed(1)+" cm³";el("download").href=data.stlUrl;el("studio").href=data.studioUrl;el("warnings").replaceChildren(...(data.warnings||[]).map(text=>{const node=document.createElement("div");node.className="warning";node.textContent=text;return node}));el("status").textContent=data.exceedsBuildVolume?"Check build volume":"Ready for slicer";try{const response=await fetch(data.stlUrl);if(!response.ok)throw 0;const geometry=new STLLoader().parse(await response.arrayBuffer());if(current!==token){geometry.dispose();return}geometry.computeVertexNormals();if(mesh){scene.remove(mesh);mesh.geometry.dispose();mesh.material.dispose()}const presets={"pla-orange":["#f35a2d",.38,.04],"pla-matte":["#e6dfcf",.78,0],"pla-silk":["#7458d8",.2,.42],petg:["#78c7dd",.16,0],resin:["#d98b32",.12,0]},preset=presets[data.materialPreset]||presets["pla-orange"];mesh=new THREE.Mesh(geometry,new THREE.MeshStandardMaterial({color:preset[0],roughness:preset[1],metalness:preset[2],emissive:data.materialPreset==="pla-orange"?"#401006":"#000000",emissiveIntensity:data.materialPreset==="pla-orange"?.12:0}));mesh.castShadow=true;mesh.receiveShadow=true;scene.add(mesh);frame()}finally{if(current===token)el("loading").style.display="none"}}
    const initial=(window.openai&&window.openai.toolOutput)||(window.openai&&window.openai.toolInput);if(initial)show(initial);window.addEventListener("message",event=>{if(event.source!==window.parent)return;const msg=event.data,data=msg&&msg.params&&msg.params.structuredContent;if(data&&(msg.method==="ui/notifications/tool-result"||msg.method==="ui/notifications/tool-input"))show(data)});el("focus").addEventListener("click",frame);function resize(){const box=el("canvas").getBoundingClientRect();renderer.setSize(box.width,box.height,false);camera.aspect=box.width/Math.max(box.height,1);camera.updateProjectionMatrix()}new ResizeObserver(resize).observe(el("canvas"));resize();(function loop(){requestAnimationFrame(loop);controls.update();renderer.render(scene,camera)})();
  </script>
</body>
</html>`;
}
