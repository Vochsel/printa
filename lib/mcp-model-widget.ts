export function createModelWidgetHtml(origin: string) {
  return String.raw`<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <style>
    :root{color-scheme:light;--canvas:#fffaf0;--soft:#faf5e8;--card:#f5f0e0;--strong:#ebe6d6;--ink:#0a0a0a;--body:#3a3a3a;--muted:#6a6a6a;--hair:#e5dfd0;--pink:#ff4d8b;--teal:#1a3a3a;--lav:#b8a4ed;--peach:#ffb084;--mint:#a4d4c5}*{box-sizing:border-box}body{margin:0;background:transparent;color:var(--ink);font-family:Inter,ui-sans-serif,system-ui,-apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif}.app{height:580px;min-height:520px;display:grid;grid-template-columns:300px minmax(0,1fr);overflow:hidden;border:1px solid var(--hair);border-radius:16px;background:var(--canvas)}.side{min-height:0;display:flex;flex-direction:column;border-right:1px solid var(--hair);background:var(--soft)}.side-head{padding:18px 18px 14px}.brand{display:flex;align-items:center;gap:9px;font-size:12px;font-weight:700;letter-spacing:.08em}.mark{display:grid;place-items:center;width:30px;height:30px;border-radius:9px;background:var(--ink);color:#fff}.tag{margin-left:auto;padding:4px 7px;border-radius:999px;background:var(--card);color:var(--muted);font-size:8px;font-weight:700;letter-spacing:.06em}.kicker{display:flex;align-items:center;gap:6px;margin-top:16px;color:var(--pink);font-size:9px;font-weight:700;letter-spacing:.1em;text-transform:uppercase}.side h1{margin:6px 0 6px;font-size:23px;line-height:1.04;letter-spacing:-.035em;font-weight:600}.side p{margin:0;color:var(--muted);font-size:10px;line-height:1.5}.mode-map{display:flex;gap:5px;margin:14px 18px 0;padding:4px;border-radius:12px;background:var(--strong)}.mode-map span{flex:1;padding:7px 5px;border-radius:9px;color:var(--muted);text-align:center;font-size:8px;font-weight:700}.mode-map span:first-child{background:var(--canvas);color:var(--ink)}.controls{min-height:0;display:flex;flex:1;flex-direction:column;gap:12px;padding:14px 18px 18px;overflow:auto}.label{display:flex;align-items:center;justify-content:space-between;margin-bottom:6px;color:var(--body);font-size:9px;font-weight:700}.label small{color:var(--muted);font-size:8px;font-weight:600}.select-wrap{position:relative}.select-wrap:after{content:"⌄";position:absolute;right:12px;bottom:12px;color:var(--muted);pointer-events:none}select{width:100%;height:42px;appearance:none;padding:0 34px 0 12px;border:1px solid var(--hair);border-radius:12px;outline:0;background:var(--canvas);color:var(--ink);font-size:10px;font-weight:650}select:focus,textarea:focus{border-color:var(--lav);box-shadow:0 0 0 3px rgba(184,164,237,.22)}textarea{width:100%;min-height:178px;resize:vertical;padding:12px;border:1px solid #302f2b;border-radius:12px;outline:0;background:#161c1b;color:#f6f0e4;caret-color:var(--pink);font:500 9px/1.5 ui-monospace,SFMono-Regular,Menlo,monospace;tab-size:2}.error{display:none;padding:9px 10px;border-radius:10px;background:#fff0e7;color:#a43b26;font-size:9px;line-height:1.4}.apply{min-height:44px;display:flex;align-items:center;justify-content:center;gap:8px;border:0;border-radius:12px;background:var(--ink);color:#fff;font-size:11px;font-weight:700;cursor:pointer}.apply:hover{background:#282828}.apply:disabled{opacity:.55;cursor:wait}.apply i{display:none;width:12px;height:12px;border:2px solid #fff;border-top-color:transparent;border-radius:50%;animation:spin .8s linear infinite}.apply.is-loading i{display:block}.hint{color:var(--muted);font-size:8px;line-height:1.45}.stage{min-width:0;min-height:0;display:grid;grid-template-rows:auto minmax(0,1fr) auto;padding:18px;background:var(--canvas)}.stage-head{display:flex;align-items:flex-start;justify-content:space-between;gap:16px;padding:0 2px 14px}.stage-head small{display:block;margin-bottom:5px;color:var(--pink);font-size:8px;font-weight:700;letter-spacing:.1em;text-transform:uppercase}.stage-head strong{display:block;max-width:420px;overflow:hidden;text-overflow:ellipsis;white-space:nowrap;font-size:18px;font-weight:600;letter-spacing:-.025em}.download{height:40px;display:inline-flex;align-items:center;justify-content:center;padding:0 14px;border-radius:12px;background:var(--ink);color:#fff;text-decoration:none;font-size:10px;font-weight:700}.viewer{position:relative;min-height:0;overflow:hidden;border-radius:20px;background:#102321}.canvas{position:absolute;inset:0}.canvas canvas{display:block;width:100%;height:100%}.hud{position:absolute;z-index:2;inset:14px 14px auto;display:flex;justify-content:space-between;gap:10px;pointer-events:none}.pill{padding:7px 10px;border:1px solid rgba(255,255,255,.14);border-radius:999px;background:rgba(10,20,19,.72);color:#d9d7cf;font-size:8px;font-weight:600;backdrop-filter:blur(8px)}.focus{position:absolute;z-index:3;right:14px;bottom:14px;width:36px;height:36px;border:1px solid rgba(255,255,255,.15);border-radius:11px;background:rgba(10,20,19,.76);color:#fff;cursor:pointer}.loading{position:absolute;z-index:4;inset:0;display:grid;place-items:center;background:rgba(16,35,33,.68);color:#fff;font-size:10px;font-weight:600;backdrop-filter:blur(4px)}.stage-foot{min-height:58px;display:flex;align-items:center;gap:22px;padding:12px 2px 0}.stat{display:grid;gap:3px}.stat small{color:var(--muted);font-size:8px;font-weight:600;text-transform:uppercase;letter-spacing:.06em}.stat strong{color:var(--body);font-size:10px;font-weight:650}.status{margin-left:auto;display:flex;align-items:center;gap:6px;padding:7px 10px;border-radius:999px;background:#eaf6ef;color:#287548;font-size:8px;font-weight:700}.status i{width:6px;height:6px;border-radius:50%;background:#2e9d5d}.warnings{position:absolute;z-index:3;left:14px;right:64px;bottom:14px;display:grid;gap:5px;pointer-events:none}.warning{width:max-content;max-width:100%;padding:7px 9px;border-radius:9px;background:#fff0df;color:#9a5223;font-size:8px;font-weight:650}.editor-link{color:var(--muted);font-size:9px;font-weight:650;text-decoration:none}.editor-link:hover{color:var(--ink)}@keyframes spin{to{transform:rotate(360deg)}}@media(max-width:720px){.app{height:auto;min-height:680px;grid-template-columns:1fr;grid-template-rows:auto 430px}.side{border-right:0;border-bottom:1px solid var(--hair)}.controls{display:grid;grid-template-columns:1fr 1.2fr;overflow:visible}.controls .apply,.controls .hint,.controls .error{grid-column:1/-1}.stage{padding:14px}.stage-foot{gap:10px;flex-wrap:wrap}.status{margin-left:0}.mode-map{display:none}}@media(max-width:480px){.controls{grid-template-columns:1fr}.app{border-radius:0}.stage-head strong{font-size:15px}.stage-foot .stat:nth-child(3){display:none}}
  </style>
</head>
<body>
  <div class="app">
    <aside class="side">
      <div class="side-head"><div class="brand"><span class="mark">▰</span> PRINTA <span class="tag">SPEC 1.0</span></div><span class="kicker">✦ Composable model graph</span><h1>Shape in layers.</h1><p>Choose a starting form or edit the complete JSON/YAML spec.</p></div>
      <div class="mode-map"><span>Source</span><span>Modifiers</span><span>Output</span></div>
      <div class="controls">
        <label class="select-wrap"><span class="label"><span>Starting form</span><small>9 demos</small></span><select id="demo"><option value="type-specimen">Type specimen</option><option value="contour-spiral-vase">Contour spiral vase</option><option value="zenith-twist">Zenith twist vase</option><option value="fluted-bud-vase">Fluted bud vase</option><option value="ripple-column-vase">Ripple column vase</option><option value="spline-petal-dish">Spline petal dish</option><option value="primitive-totem">Primitive totem</option><option value="water-ripple-tile">Water ripple tile</option><option value="cloth-drape-study">Cloth drape study</option></select></label>
        <label><span class="label"><span>JSON / YAML spec</span><small id="chars">0 chars</small></span><textarea id="spec" spellcheck="false" aria-label="Procedural model JSON or YAML spec"></textarea></label>
        <div id="error" class="error"></div>
        <button id="apply" class="apply" type="button"><i></i><span>Apply spec</span></button>
        <div class="hint">Changes call <b>create_procedural_model</b> again, so the ChatGPT result and downloadable STL stay in sync.</div>
      </div>
    </aside>
    <main class="stage">
      <header class="stage-head"><div><small>Generated printable solid</small><strong id="title">Building model…</strong></div><a id="download" class="download" href="#">Download STL</a></header>
      <div class="viewer"><div id="canvas" class="canvas"></div><div class="hud"><span class="pill">Drag to orbit · scroll to zoom</span><span id="dims" class="pill">—</span></div><button id="focus" class="focus" aria-label="Frame model">◎</button><div id="warnings" class="warnings"></div><div id="loading" class="loading">Evaluating model graph…</div></div>
      <footer class="stage-foot"><span class="stat"><small>Triangles</small><strong id="triangles">—</strong></span><span class="stat"><small>Volume est.</small><strong id="volume">—</strong></span><span class="stat"><small>Material</small><strong id="material">—</strong></span><a id="editor" class="editor-link" href="#">Open full editor ↗</a><span id="status" class="status"><i></i><span>Ready for slicer</span></span></footer>
    </main>
  </div>
  <script type="module">
    import * as THREE from "https://cdn.jsdelivr.net/npm/three@0.185.0/build/three.module.js";
    import {OrbitControls} from "https://cdn.jsdelivr.net/npm/three@0.185.0/examples/jsm/controls/OrbitControls.js";
    import {STLLoader} from "https://cdn.jsdelivr.net/npm/three@0.185.0/examples/jsm/loaders/STLLoader.js";
    const ORIGIN=${JSON.stringify(origin)},el=id=>document.getElementById(id),scene=new THREE.Scene();scene.background=new THREE.Color("#102321");const camera=new THREE.PerspectiveCamera(34,1,.1,3000);camera.up.set(0,0,1);camera.position.set(145,-185,125);const renderer=new THREE.WebGLRenderer({antialias:true,powerPreference:"high-performance"});renderer.setPixelRatio(Math.min(devicePixelRatio,2));renderer.outputColorSpace=THREE.SRGBColorSpace;renderer.shadowMap.enabled=true;renderer.toneMapping=THREE.ACESFilmicToneMapping;renderer.toneMappingExposure=1.08;el("canvas").appendChild(renderer.domElement);const controls=new OrbitControls(camera,renderer.domElement);controls.enableDamping=true;controls.dampingFactor=.065;controls.target.set(0,0,45);scene.add(new THREE.HemisphereLight("#fff8e9","#173534",2.8));const key=new THREE.DirectionalLight("#fff0d7",5.4);key.position.set(-120,-140,220);key.castShadow=true;scene.add(key);const rim=new THREE.DirectionalLight("#ff7daa",3.6);rim.position.set(130,90,150);scene.add(rim);const floor=new THREE.Mesh(new THREE.CircleGeometry(230,96),new THREE.MeshStandardMaterial({color:"#18312f",roughness:.88,metalness:.04}));floor.position.z=-.25;floor.receiveShadow=true;scene.add(floor);const grid=new THREE.GridHelper(400,40,"#3c5b57","#29423f");grid.rotation.x=Math.PI/2;grid.position.z=.04;scene.add(grid);let mesh=null,dimensions=null,token=0,seq=0;const pending=new Map();
    function frame(){if(!mesh)return;const box=new THREE.Box3().setFromObject(mesh);if(dimensions)box.expandByObject(dimensions);const sphere=box.getBoundingSphere(new THREE.Sphere()),distance=Math.max(38,sphere.radius/Math.tan(THREE.MathUtils.degToRad(camera.fov/2))*1.12);camera.position.set(sphere.center.x+distance*.75,sphere.center.y-distance,sphere.center.z+distance*.62);camera.near=Math.max(.1,distance/150);camera.far=distance*20;camera.updateProjectionMatrix();controls.target.copy(sphere.center);controls.update()}
    function disposeObject(object){if(!object)return;object.traverse(child=>{if(child.geometry)child.geometry.dispose();const list=child.material?(Array.isArray(child.material)?child.material:[child.material]):[];list.forEach(material=>{if(material.map)material.map.dispose();material.dispose()})})}
    function dimensionLabel(text,color,size){const canvas=document.createElement("canvas");canvas.width=512;canvas.height=128;const c=canvas.getContext("2d");c.fillStyle="rgba(10,26,25,.92)";c.beginPath();c.roundRect(3,3,506,122,24);c.fill();c.strokeStyle=color;c.lineWidth=5;c.stroke();c.fillStyle="#fffaf0";c.font="700 48px ui-monospace,monospace";c.textAlign="center";c.textBaseline="middle";c.fillText(text,256,65);const texture=new THREE.CanvasTexture(canvas);texture.colorSpace=THREE.SRGBColorSpace;const label=new THREE.Mesh(new THREE.PlaneGeometry(size*4,size),new THREE.MeshBasicMaterial({map:texture,transparent:true,depthTest:false,toneMapped:false}));label.renderOrder=12;return label}
    function groundDimensions(box,settings,units){const group=new THREE.Group(),w=box.max.x-box.min.x,h=box.max.y-box.min.y,largest=Math.max(w,h),scale=units==="cm"?10:units==="in"?25.4:1,margin=Math.max((settings.offset||9)*scale,largest*.045),arrow=THREE.MathUtils.clamp(largest*.025,2.5,9),size=THREE.MathUtils.clamp(largest*.035,4,10),z=.32,widthY=box.min.y-margin,heightX=box.min.x-margin,precision=settings.precision??1;function lines(points,color){const geometry=new THREE.BufferGeometry().setFromPoints(points),line=new THREE.LineSegments(geometry,new THREE.LineBasicMaterial({color,depthTest:false}));line.renderOrder=10;group.add(line)}const v=(x,y)=>new THREE.Vector3(x,y,z);if(settings.width!==false){lines([v(box.min.x,widthY),v(box.max.x,widthY),v(box.min.x,widthY),v(box.min.x+arrow,widthY+arrow*.52),v(box.min.x,widthY),v(box.min.x+arrow,widthY-arrow*.52),v(box.max.x,widthY),v(box.max.x-arrow,widthY+arrow*.52),v(box.max.x,widthY),v(box.max.x-arrow,widthY-arrow*.52)],"#ff6b8f");const label=dimensionLabel("W  "+(w/scale).toFixed(precision)+" "+units,"#ff6b8f",size);label.position.set((box.min.x+box.max.x)/2,widthY-size*1.05,z+.03);group.add(label)}if(settings.height!==false){lines([v(heightX,box.min.y),v(heightX,box.max.y),v(heightX,box.min.y),v(heightX+arrow*.52,box.min.y+arrow),v(heightX,box.min.y),v(heightX-arrow*.52,box.min.y+arrow),v(heightX,box.max.y),v(heightX+arrow*.52,box.max.y-arrow),v(heightX,box.max.y),v(heightX-arrow*.52,box.max.y-arrow)],"#b8a4ed");const label=dimensionLabel("H  "+(h/scale).toFixed(precision)+" "+units,"#b8a4ed",size);label.rotation.z=Math.PI/2;label.position.set(heightX-size*1.05,(box.min.y+box.max.y)/2,z+.03);group.add(label)}return group}
    function requestHost(method,params,timeout=45000){return new Promise((resolve,reject)=>{const id=++seq;pending.set(id,{resolve,reject});window.parent.postMessage({jsonrpc:"2.0",id,method,params},"*");setTimeout(()=>{if(pending.has(id)){pending.delete(id);reject(new Error("Host request timed out"))}},timeout)})}function callTool(name,args){return requestHost("tools/call",{name,arguments:args})}
    function setBusy(busy,label){el("apply").disabled=busy;el("apply").classList.toggle("is-loading",busy);el("apply").querySelector("span").textContent=label||"Apply spec";if(busy)el("loading").style.display="grid"}
    async function show(data,replaceSpec=true){if(!data||!data.stlUrl)return;const current=++token;el("loading").style.display="grid";el("title").textContent=data.name||"Procedural model";el("dims").textContent=Number(data.widthMm).toFixed(1)+" × "+Number(data.depthMm).toFixed(1)+" × "+Number(data.heightMm).toFixed(1)+" mm";el("triangles").textContent=Number(data.triangles).toLocaleString();el("volume").textContent=(Number(data.volumeEstimateMm3)/1000).toFixed(1)+" cm³";el("material").textContent=(data.materialPreset||"PLA").replaceAll("-"," ");el("download").href=data.stlUrl;el("editor").href=data.studioUrl||ORIGIN+"/editor";if(replaceSpec&&data.spec){el("spec").value=data.spec;el("chars").textContent=data.spec.length.toLocaleString()+" chars"}el("warnings").replaceChildren(...(data.warnings||[]).map(text=>{const node=document.createElement("div");node.className="warning";node.textContent=text;return node}));el("status").classList.toggle("is-warning",!!data.exceedsBuildVolume);el("status").querySelector("span").textContent=data.exceedsBuildVolume?"Check build volume":"Ready for slicer";try{const response=await fetch(data.stlUrl);if(!response.ok)throw new Error("Model could not be loaded");const geometry=new STLLoader().parse(await response.arrayBuffer());if(current!==token){geometry.dispose();return}geometry.computeVertexNormals();geometry.computeBoundingBox();if(mesh){scene.remove(mesh);mesh.geometry.dispose();mesh.material.dispose()}if(dimensions){scene.remove(dimensions);disposeObject(dimensions);dimensions=null}const presets={"pla-orange":["#ff6b5a",.4,.03],"pla-matte":["#ebe6d6",.8,0],"pla-silk":["#b8a4ed",.22,.36],petg:["#a4d4c5",.2,0],resin:["#ffb084",.16,0]},preset=presets[data.materialPreset]||presets["pla-orange"];mesh=new THREE.Mesh(geometry,new THREE.MeshPhysicalMaterial({color:preset[0],roughness:preset[1],metalness:preset[2],clearcoat:data.materialPreset==="resin"?.7:.15}));mesh.castShadow=true;mesh.receiveShadow=true;scene.add(mesh);const display=data.display||{floor:true,grid:true,dimensions:{visible:true,width:true,height:true,offset:9,precision:1}};floor.visible=display.floor!==false;grid.visible=display.grid!==false;if(display.dimensions&&display.dimensions.visible!==false&&geometry.boundingBox){dimensions=groundDimensions(geometry.boundingBox,display.dimensions,data.units||"mm");scene.add(dimensions)}frame();el("error").style.display="none"}catch(error){el("error").textContent=error.message||"Model could not be loaded.";el("error").style.display="block"}finally{if(current===token){el("loading").style.display="none";setBusy(false)}}}
    async function run(args){setBusy(true,"Building…");el("error").style.display="none";try{const result=await callTool("create_procedural_model",args),data=result&&result.structuredContent;if(data)await show(data,true)}catch(error){el("error").textContent=error.message||error.data&&error.data.message||"Could not build this spec.";el("error").style.display="block";setBusy(false)}}
    el("apply").addEventListener("click",()=>{const spec=el("spec").value.trim();if(spec)run({spec})});el("demo").addEventListener("change",event=>run({demo:event.target.value}));el("spec").addEventListener("input",event=>{el("chars").textContent=event.target.value.length.toLocaleString()+" chars"});el("focus").addEventListener("click",frame);
    window.addEventListener("message",event=>{if(event.source!==window.parent)return;const msg=event.data;if(!msg||msg.jsonrpc!=="2.0")return;if(msg.id&&pending.has(msg.id)){const p=pending.get(msg.id);pending.delete(msg.id);msg.error?p.reject(msg.error):p.resolve(msg.result);return}const data=msg.params&&msg.params.structuredContent;if(data&&(msg.method==="ui/notifications/tool-result"||msg.method==="ui/notifications/tool-input"))show(data,true)});const initial=(window.openai&&window.openai.toolOutput)||(window.openai&&window.openai.toolInput);if(initial)show(initial,true);function resize(){const box=el("canvas").getBoundingClientRect();renderer.setSize(box.width,box.height,false);camera.aspect=box.width/Math.max(box.height,1);camera.updateProjectionMatrix()}new ResizeObserver(resize).observe(el("canvas"));resize();(function loop(){requestAnimationFrame(loop);controls.update();renderer.render(scene,camera)})();
  </script>
</body>
</html>`;
}
